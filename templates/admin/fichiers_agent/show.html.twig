{% extends 'base.html.twig' %}

{% block title %}FichiersAgent{% endblock %}

{% block body %}
    <h1>FichiersAgent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fichiers_agent.id }}</td>
            </tr>
            <tr>
                <th>Nom_fichier</th>
                <td>{{ fichiers_agent.Fichier }}</td>
            </tr>
            <tr>
                <th>Numero_dossier</th>
                <td>{{ fichiers_agent.nomFichier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_fichiers_agent_index') }}">back to list</a>

    <a href="{{ path('app_admin_fichiers_agent_edit', {'id': fichiers_agent.id}) }}">edit</a>

    {{ include('admin/fichiers_agent/_delete_form.html.twig') }}
{% endblock %}
