{% extends "./admin/adminbase.html.twig" %}
{% block title %}Statut Agent | {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
      <div class="col-md-12 col-sm-12 ">
        <div class="x_panel">
          <div class="x_title">
            <h2>Statut Agent</h2>
            <ul class="nav navbar-right panel_toolbox">
              <li>
                  <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
              </li>
              <li>
                  <a href="{{ path('app_admin_statut_agent_show', {'id':statut_agent.id}) }}" title="Accueil Agent" ><i class="fas fa-fw fa-home"></i></a>
              </li>
              <li>
                  <a class="close-link"><i class="fa fa-close"></i></a>
              </li>
            </ul>
            <div class="clearfix"></div>
          </div>
          <div class="x_content">
            <div class="row">
              <div class="col-sm-12">
                <div class="card-box table-responsive">
                  <p class="text-muted font-13 m-b-30">
                    Suppression Statut Agent
                  </p>
                  <div class="card">
                    <div class="card-body">
                      <div class="row">
                        <div class="col-md-10 col-xs-12 mr-2">
                          <div class="card o-hidden border-0 shadow-lg my-5">
                            <div class="card-body p-1">
                              <div class="row">
                                <div class="col-lg-10 mx-auto">
                                  <div class="p-5">
                                    <p>Supprimer le statut de l'agent <strong class="text-danger"> {{statut_agent.agent.Prenom}} {{ statut_agent.agent.Nom }} {{statut_agent.agent.Matricule}} </strong>  ?</p>
                                    <form method="post" action="{{ path('app_admin_statut_agent_delete', {'id': statut_agent.id}) }}" onsubmit="return confirm(Êtes-vous sûr de vouloir effectuer cette action ?');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut_agent.id) }}">
                                        <button class="btn btn-danger">Supprimer <i class="fas fa-trash-alt"></i></button>
                                    </form>
                                  </div>
                                </div>
                              </div>
                            </div>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

{% endblock %}