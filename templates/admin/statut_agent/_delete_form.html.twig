<form method="post" action="{{ path('app_admin_statut_agent_delete', {'id': statut_agent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut_agent.id) }}">
    <button class="btn">Delete</button>
</form>
