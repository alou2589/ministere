{% extends './admin/adminbase.html.twig' %}

{% block title %}{{categorie_materiel.NomCategorieMatos}} | {{ parent() }}{% endblock %}

{% block body %}
    <div class="row">
      <div class="col-md-12 col-sm-12 ">
        <div class="x_panel">
          <div class="x_title">
            <h2>{{categorie_materiel.NomCategorieMatos}}</h2>
            <ul class="nav navbar-right panel_toolbox">
              <li>
                  <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
              </li>
              <li>
                  <a href="{{ path('app_admin_categorie_materiel_show', {'id': categorie_materiel.id}) }}" title="{{ categorie_materiel.NomCategorieMatos }}" ><i class="fas fa-fw fa-home"></i></a>
              </li>
              <li>
                  <a class="close-link"><i class="fa fa-close"></i></a>
              </li>
            </ul>
            <div class="clearfix"></div>
          </div>
          <div class="x_content">
            <div class="row">
              <div class="col-sm-12">
                <div class="card-box table-responsive">
                    <p class="text-muted font-13 m-b-30">
                        Liste des <strong class="text-primary">{{categorie_materiel.NomCategorieMatos}}s</strong> 
                    </p>
                  <table id="datatable-buttons" class="table table-striped table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th class="text-center">Id</th>
                            <th class="text-center">Type</th>
                            <th class="text-center">Marque</th>
                            <th class="text-center">Modele</th>
                            <th class="text-center">Information</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for type_mato in categorie_materiel.typeMateriels %}
                        {% for matos in type_mato.materiels %}
                            <tr>
                                <td class="text-center">{{ matos.id }}</td>
                                <td class="text-center">{{ matos.typeMatos.NomTypeMatos }}</td>
                                <td class="text-center">{{matos.marqueMatos.NomMarqueMatos}}</td>
                                <td class="text-center">{{matos.ModeleMatos}}</td>
                                <td class="text-center">
                                    <a href="{{ path('app_admin_materiel_show', {'id': matos.id}) }}"><i class="fas fa-eye text-info" title="Détails"></i></a>
                                </td>
                            </tr>
                        {% endfor %}
                        {% else %}
                            <tr>
                              <td class="text-center" colspan="5">
                                Pas de données
                              </td>
                            </tr>
                            
                    {% endfor %}
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
{% endblock %}
