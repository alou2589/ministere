
<table id="datatable-buttons" class="table table-striped table-bordered" style="width:100%">
  <thead>
      <tr>
          <th class="text-center">Id</th>
          <th class="text-center">Prenom</th>
          <th class="text-center">Nom</th>
          <th class="text-center">Genre</th>
          <th class="text-center">Matricule</th>
          <th class="text-center">Information</th>
      </tr>
  </thead>
  <tbody>
      {% for statut_agent in type_agent.statutAgents %}
          <tr>
              <td class="text-center">{{ statut_agent.agent.id }}</td>
              <td class="text-center">{{ statut_agent.agent.Prenom }}</td>
              <td class="text-center">{{statut_agent.agent.Nom}}</td>
              <td class="text-center">{{statut_agent.agent.Genre}}</td>
              <td class="text-center">{{statut_agent.Matricule}}</td>
              <td class="text-center">
                  <a href="{{ path('app_admin_statut_agent_show', {'id': statut_agent.id}) }}"><i class="fas fa-eye text-info" title="Détails"></i></a>
              </td>
          </tr>
      {% else %}
          <tr>
              <td class="text-center" colspan="7">Pas de données</td>
          </tr>
          
    {% endfor %}
  </tbody>
</table>
