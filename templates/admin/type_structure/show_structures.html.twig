{#
<table id="datatable-buttons" class="table table-striped table-bordered" style="width:100%">
    <thead>
    <tr>
        <th class="text-center">Id</th>
        <th class="text-center">Nom Type de Véhicule</th>
        <th class="text-center">Information</th>
    </tr>
    </thead>
    <tbody>
    {% for type_vehicule in type_vehicule.vehicules %}
        <tr>
            <td class="text-center">{{ type_vehicule.id }}</td>
            <td class="text-center">{{ type_vehicule.NomStructure }}</td>
            <td class="text-center">
                <a href="{{ path('app_admin_structure_show', {'id': structure.id}) }}"><i class="fas fa-eye text-info" title="Détails"></i></a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td class="text-center" colspan="5">Pas de données</td>
        </tr>

    {% endfor %}
    </tbody>
</table>
#}